import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/">
      <img src="/public/book-svgrepo-com-colors.svg" alt="icone de um livro" />
    </Link>
  );
};

export default Logo;
